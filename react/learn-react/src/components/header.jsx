import React from "react";

function Headers() {
    return(
        <div>
            <p>Hello World!</p>
            <p>{Math.floor(Math.random()* 100)}</p>
        </div>
    );
};

export default Headers ;